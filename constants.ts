import { LocalModel } from './types';

export const INITIAL_GREETING = `**Airlock Initialized.**

I am your local AI assistant. All processing is performed offline within this secure environment.

**Capabilities:**
- Answer questions using local models found in \`/models\`.
- Modify JSON configuration files safely.
- No outbound data connections.

To begin, select a model from the top bar or upload a config file.`;
